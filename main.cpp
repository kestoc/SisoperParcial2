#include<bits/stdc++.h>
#include "priority.h"
#include "fcfs.h"
using namespace std;

int main(){
    //Priority Example
    /*Process proc[] = {{1, 10, 2}, {2, 5, 0}, {3, 8, 1}};
    int n = sizeof proc / sizeof proc[0];
    priorityScheduling(proc, n);*/

    //FCFS Example
    /*process proc[] = {{2, 1, 3}, {1, 0, 4}, {4, 3, 2}, {3, 2, 1}, {5, 4, 5}};
    int N = sizeof proc/sizeof proc[0];
    fcfsScheduling(proc, N);*/
    return 0;
}