#include<bits/stdc++.h>
#include <thread>
#include "priority.h"
#include "fcfs.h"
#include "sjf.h"
using namespace std;

int main(){
    /*
    //Priority Example
    process1 proc1[] = {{5, 11, 7}, {4, 6, 9}, {3, 4, 10}, {2, 3, 5}, {1, 1, 8}};
    int N1 = sizeof proc1 / sizeof proc1[0];
    thread th1(priorityScheduling, proc1, N1); //Algorithm Priority is launched

    //FCFS Example
    process2 proc2[] = {{5, 11, 7}, {4, 6, 9}, {3, 4, 10}, {2, 3, 5}, {1, 0, 8}};
    int N2 = sizeof proc2/sizeof proc2[0];
    thread th2(fcfsScheduling, proc2, N2); //Algorithm FCFS is launched

    //SJF Example
    process3 proc3[] = {{5, 11, 7}, {4, 6, 9}, {3, 4, 10}, {2, 3, 5}, {1, 0, 8}};
    int N3 = sizeof proc3/sizeof proc3[0];
    thread th3(sjfScheduling, proc3, N3); //Algorithm SJF is launched

    th1.join() //Wait for thread t1 to finish
    th2.join() //Wait for thread t2 to finish
    th3.join() //Wait for thread t3 to finish*/

    return 0;
}